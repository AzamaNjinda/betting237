from django.test import TestCase

# Create your tests here.
# if response.status == 'PENDING':
#     await asyncio.sleep(15)  # Asynchronously wait and check the status again
#     # You can add a loop here to continuously check the status if needed
    
# if response.is_transaction_success():
#     user.account_balance += amount
#     await sync_to_async(user.save)()  # Save the user object asynchronously
#     return redirect("server:payment_successful")
# else:
#     context = {
#         'message': "ERROR: Payment Not Successful",
#         'form': form,
#     }
#     #return render(request, "dashboard-deposit.html", context)